'use client'
import React from 'react'

const AddtoCart = () => {
  return (
    <div>
      <button className='btn btn-primary' onClick={()=>console.log('Click')}>Add To Cart Product</button>
    </div>
  )
}

export default AddtoCart
